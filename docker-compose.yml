services:
  clipcut-bot:
    build: .
    image: clipcut-bot
    container_name: clipcut-bot
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./keepers:/app/keepers
      - ./fonts:/app/fonts
      - ./config_examples:/app/config_examples
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "30m"
